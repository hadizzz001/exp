/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['a-r-t.com', 'staticapp.icpsc.com','www.p-t-e.org' , 'p-t-e.org','ui.icontact.com','images.ctfassets.net'], // Add other domains here if needed
      },
};

export default nextConfig;
